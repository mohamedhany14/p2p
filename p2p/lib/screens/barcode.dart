import 'package:flutter/material.dart';

class barcode extends StatelessWidget {
  const barcode({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(height: 500,width: 500,color: Colors.black,),
    );
  }
}