import 'package:flutter/material.dart';

class recieve extends StatelessWidget {
  const recieve({super.key});

  @override
  Widget build(BuildContext context) {
     return Scaffold(
      body: Container(height: 500,width: 500,color: Colors.yellow,),
    );
  }
}